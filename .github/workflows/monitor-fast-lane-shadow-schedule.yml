name: Monitor fast lane shadow schedule

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/monitor-fast-lane-shadow-schedule.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - run: mkdir -p fast-lane-shadow-monitor-evidence

      - name: Resolve Worker URL
        run: |
          curl --fail-with-body --silent --show-error \
            -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/workers/subdomain" \
            > fast-lane-shadow-monitor-evidence/subdomain.json
          SUBDOMAIN="$(jq -r '.result.subdomain' fast-lane-shadow-monitor-evidence/subdomain.json)"
          test -n "${SUBDOMAIN}"
          printf '%s\n' "https://xrpl-lending-fast-lane-shadow.${SUBDOMAIN}.workers.dev" \
            > fast-lane-shadow-monitor-evidence/base-url.txt

      - name: Wait for cron schedule registration
        shell: bash
        run: |
          set -euo pipefail
          for ATTEMPT in $(seq 1 30); do
            curl --fail-with-body --silent --show-error \
              -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
              "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/workers/scripts/xrpl-lending-fast-lane-shadow/schedules" \
              > fast-lane-shadow-monitor-evidence/schedules-latest.json
            if jq -e '.result.schedules | any(.cron == "*/5 * * * *")' \
              fast-lane-shadow-monitor-evidence/schedules-latest.json > /dev/null; then
              cp fast-lane-shadow-monitor-evidence/schedules-latest.json \
                fast-lane-shadow-monitor-evidence/schedules-confirmed.json
              break
            fi
            sleep 30
          done
          test -f fast-lane-shadow-monitor-evidence/schedules-confirmed.json

      - name: Wait for first scheduled state
        shell: bash
        run: |
          set -euo pipefail
          BASE="$(cat fast-lane-shadow-monitor-evidence/base-url.txt)"
          for ATTEMPT in $(seq 1 20); do
            curl --fail-with-body --silent --show-error \
              "${BASE}/status" > fast-lane-shadow-monitor-evidence/status-latest.json
            LAST_LEDGER="$(jq -r '.state.last_processed_ledger // 0' fast-lane-shadow-monitor-evidence/status-latest.json)"
            UPDATED_AT="$(jq -r '.state.updated_at // empty' fast-lane-shadow-monitor-evidence/status-latest.json)"
            if [ "${LAST_LEDGER}" -gt 0 ] && [ -n "${UPDATED_AT}" ]; then
              cp fast-lane-shadow-monitor-evidence/status-latest.json \
                fast-lane-shadow-monitor-evidence/status-first.json
              break
            fi
            sleep 30
          done
          test -f fast-lane-shadow-monitor-evidence/status-first.json

      - name: Wait for second scheduled cursor advance
        shell: bash
        run: |
          set -euo pipefail
          BASE="$(cat fast-lane-shadow-monitor-evidence/base-url.txt)"
          FIRST_LEDGER="$(jq -r '.state.last_processed_ledger' fast-lane-shadow-monitor-evidence/status-first.json)"
          FIRST_UPDATED="$(jq -r '.state.updated_at' fast-lane-shadow-monitor-evidence/status-first.json)"
          FIRST_MS="$(node -e 'process.stdout.write(String(Date.parse(process.argv[1])))' "${FIRST_UPDATED}")"

          for ATTEMPT in $(seq 1 20); do
            sleep 30
            curl --fail-with-body --silent --show-error \
              "${BASE}/status" > fast-lane-shadow-monitor-evidence/status-latest.json
            LAST_LEDGER="$(jq -r '.state.last_processed_ledger // 0' fast-lane-shadow-monitor-evidence/status-latest.json)"
            UPDATED_AT="$(jq -r '.state.updated_at // empty' fast-lane-shadow-monitor-evidence/status-latest.json)"
            UPDATED_MS="$(node -e 'const v=Date.parse(process.argv[1]||""); process.stdout.write(Number.isFinite(v)?String(v):"0")' "${UPDATED_AT}")"
            if [ "${LAST_LEDGER}" -gt "${FIRST_LEDGER}" ] && [ "${UPDATED_MS}" -gt "${FIRST_MS}" ]; then
              cp fast-lane-shadow-monitor-evidence/status-latest.json \
                fast-lane-shadow-monitor-evidence/status-second.json
              break
            fi
          done
          test -f fast-lane-shadow-monitor-evidence/status-second.json

      - name: Build retained monitor summary
        shell: bash
        run: |
          set -euo pipefail
          FIRST_LEDGER="$(jq -r '.state.last_processed_ledger' fast-lane-shadow-monitor-evidence/status-first.json)"
          SECOND_LEDGER="$(jq -r '.state.last_processed_ledger' fast-lane-shadow-monitor-evidence/status-second.json)"
          FIRST_UPDATED="$(jq -r '.state.updated_at' fast-lane-shadow-monitor-evidence/status-first.json)"
          SECOND_UPDATED="$(jq -r '.state.updated_at' fast-lane-shadow-monitor-evidence/status-second.json)"
          GAP_SECONDS="$(node -e 'process.stdout.write(String((Date.parse(process.argv[2])-Date.parse(process.argv[1]))/1000))' "${FIRST_UPDATED}" "${SECOND_UPDATED}")"
          jq -n \
            --argjson first_ledger "${FIRST_LEDGER}" \
            --argjson second_ledger "${SECOND_LEDGER}" \
            --arg first_updated_at "${FIRST_UPDATED}" \
            --arg second_updated_at "${SECOND_UPDATED}" \
            --argjson observed_gap_seconds "${GAP_SECONDS}" \
            '{schemaVersion:1,passed:true,first_ledger:$first_ledger,second_ledger:$second_ledger,ledger_advance:($second_ledger-$first_ledger),first_updated_at:$first_updated_at,second_updated_at:$second_updated_at,observed_gap_seconds:$observed_gap_seconds}' \
            > fast-lane-shadow-monitor-evidence/summary.json
          cat fast-lane-shadow-monitor-evidence/summary.json

      - name: Upload retained monitor evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: fast-lane-shadow-monitor-evidence
          path: fast-lane-shadow-monitor-evidence
          if-no-files-found: warn
          retention-days: 14
